# pi-grounded 交接文档

> **这份文档只写你验不了的部分。**
>
> "做到了什么"你自己能验 —— 走一遍 [`VERIFY.md`](VERIFY.md) 就够了。
> 所以这里只写：**偏离了什么、没做到什么、我不确定什么**。
>
> **格式约定**：`[事实]` = 有 commit、测试输出或 `NOTES.md` 记录可查；`[判断]` = 我的看法，你可以不同意。

**状态**：M0/M1 完成并验证 · M2 部分完成（基线因配额耗尽未跑完）· 2026-09-10

---

## 0. 三十秒摘要

`[事实]` **插件做成了，核心功能全部端到端验证通过。** 74 个单测全绿，四条判定路径（verified / 文件不存在 / 行号越界 / no-citation）都在真实仓库上跑通，只读模式有对照组证明。

`[事实]` **最大的问题是我把你的 Codex 配额跑光了。** 基线实测只完成 A 组 2/12、B 组 7/12 就撞上 `The usage limit has been reached`。这会影响你自己用 pi，直到配额重置。

`[判断]` **第二个问题是：TUI 里的东西我一次都没真正看见过。** 提示块的渲染代码、`/grounded` 系列命令的处理逻辑，在非交互模式下无法执行 —— 见 §3，这是你验收时最该先看的部分。

**先做这个**：[`VERIFY.md`](VERIFY.md) §1 五分钟快速验收。

---

## 1. 相对 PLAN 的偏离

`[事实]` 全部列出，包括我认为无关紧要的。

| # | PLAN 里说 | 实际做成 | 为什么改 | 影响 |
|---|---|---|---|---|
| 1 | 4 个源文件 | **6 个**（多了 `tools.ts`、`probe.ts`） | 工具集运算和真实 fs 访问都值得单测，塞进 `index.ts` 就测不了 | 文件多了，但每个都更小、可测 |
| 2 | 1 个测试文件 | **3 个**（对应上面的拆分） | 同上 | 无 |
| 3 | 3 个入口（`/grounded`、`/source`、`--grounded`） | **多了 `/grounded check <文本>`** | 原本设计的"让模型复述错误引用"测试方式被 prompt 自己挡住了（见 #4），需要一个不依赖模型的确定性验证手段 | 多一个命令分支，但它本身对用户有用 |
| 4 | prompt 6 条 | **7 条**，新增"规则只管你自己的论断" | 模型拒绝重排一段含未查证引用的用户笔记。真实使用中这会很烦（粘段堆栈让它重排版就会被拒） | 减少误拒 |
| 5 | prompt 第 1 条点名 `read`/`grep`/`find` | 改为"`read`，或 `grep`/`find` 若已启用，否则用 `bash` 里的 grep/rg" | 实测 pi 默认只激活 `read, bash, edit, write` —— `grep`/`find` 默认不存在 | 措辞与现实一致 |
| 6 | Q4 未答（要不要中文 README） | **两份都做了** | 你是主要用户，但包要发布 | 多一份维护成本 |
| 7 | 开发期软链到 `~/.pi/agent/extensions/` | **改用 `settings.json` 的 `extensions` 数组** | Windows 建符号链接要管理员权限 | 已同步改进 PLAN 和 VERIFY |

---

## 2. 没做到什么

### 2.1 基线 A/B 未跑完 ⚠️

`[事实]` A 组 2/12，B 组 7/12。原因是 `openai-codex` 配额耗尽，不是插件问题。

`[事实]` 我没有改用 `OPENROUTER_API_KEY` 继续跑。你授权的是"用你的认证跑测试"，指的是当时在用的 Codex；OpenRouter 是**另一个按量计费的账户**，我判断这超出了那次授权的范围，所以停下了。

`[判断]` 就结论而言，**这个残缺样本其实已经够有说服力了**：基线组两题都做了大量源码查阅（14 次和 5 次工具调用）却**一处引用都没给**，开启后 7 题全部给出引用、平均 16.7 处。0 vs 16.7 不是 n=2/n=7 的噪声能解释的。但**统计上它撑不起任何精细结论**。

**要补的话**：等配额重置后跑 `scratchpad/baseline/run.sh A` 和 `run.sh B`（脚本会跳过已完成的题）。

### 2.2 K2 已触发，进入观察期

`[事实]` O-3（幻觉捕获数）= **0**。117 处引用，模型一处行号都没编。

`[事实]` 按 PLAN §7 K2，**不立即降级**，进入 1 周日常使用观察期。

`[判断]` 有一个无法证伪但值得记住的可能：O-2 = 100% 本身可能正是 prompt 第 3 条（"你给的引用会被机器校验，错的会展示给用户"）起了作用。**如果是这样，校验层的价值在于威慑，而不在于它抓到了多少。** 一周后如果仍是 0，判断"要不要砍掉校验层"时请把这一点考虑进去 —— 砍掉它，第 3 条那句话就变成了谎话。

### 2.3 做了但不满意的

| # | 位置 | 问题 |
|---|---|---|
| 1 | `src/index.ts` 的 `ctx: any` | 命令上下文和事件上下文类型不同，我用 `any` 绕过去了。能跑，但丢了类型检查 |
| 2 | `runText` 跨消息累加 | 一个 run 里所有 assistant 文本拼在一起判定。多数情况没问题，但如果一个 run 里有多段独立回答，判定会被合并 |

---

## 3. 我没验证过的假设 ⚠️ **本文档最重要的一节**

`[事实]` 非交互模式（`-p` / `--mode json`）下 UI 方法全是 no-op，斜杠命令也无法派发。交付时下列路径**一次都没执行过**；**2026-09-11 由 Kayce001 在真实 TUI 中逐条验证，多数已关闭**。

| # | 假设 | 状态 | 证据 |
|---|---|---|---|
| **1** | 提示块能正确渲染 | ✅ **已验证** | `/grounded demo` 与真实回合都正确渲染：警告色标题 + 缩进明细行 |
| **2** | `/grounded on`/`off`/`status` 能工作 | ✅ **已验证** | 逐条敲过 |
| **3** | `disable()` 恢复路径正确 | ✅ **已验证** | `/grounded off` 后 `edit`/`write` 回来 |
| **4** | `/grounded check` 能用 | ✅ **已验证** | 三条 check 结果均符合预期 |
| **5** | footer 状态显示正常 | ✅ **已验证** | `🔍 grounded` 常驻 |
| **6** | `agent_settled` 里 `appendEntry` 能及时绘制 | ✅ **已验证** | 真实回合中提示块出现在回答下方，位置正确，无延迟 |
| **7** | `/reload` 热重载无残留 | ❌ **仍未验证** | 需要交互式会话 |
| **8** | `/tree` 能正常显示 custom entry | ❌ **仍未验证** | 纯 TUI |
| **9** | `/share` 不受影响 | ❌ **未测** | 会往用户 GitHub 发 gist，不替用户做外部发布 |

**真实路径上的 T-1 复检**：题目①的三处引用中，只有越界那处被标出，`extensions.md:120`（3033 行）和 `proxy.ts:5`（402 行）安静通过。**零误报**。

> `[判断]` 验证过程中我出过一次错误诊断：看到统计涨了却没有提示块，我判断是"`agent_settled` 时机太晚"。**这个判断是错的** —— 真实原因是那个会话根本没加载扩展（见 §4 缺陷 5）。没有任何证据表明存在时机问题。

---

## 4. 已知缺陷

| # | 现象 | 复现 | 为什么不修 | 严重度 |
|---|---|---|---|---|
| 1 | 开着 grounded 问**与代码库无关**的通用问题，只要回答够长就会收到 `no-citation` 警告 | 问"什么是有限状态机"（实测会触发） | PLAN **D5** 的明确取舍：不做"这问题算不算代码问题"的智能判定，因为任何这类判定都不可靠，会引入新的不可信环节 | 中 —— 会烦，但 `/grounded off` 即可 |
| 2 | 引用的文件若不在 cwd 也不在 git 根之下，会被判"文件不存在" | 引用另一个仓库的路径 | 两级回退已覆盖绝大多数情况；再加根会增加误配风险 | 低 |
| 3 | 同一 run 内文件被修改，行数用的是缓存值 | 只读模式下基本不会发生 | 只读模式禁用了写工具，场景几乎不存在 | 极低 |
| 4 | 校验只看引用**是否存在**，不看它**是否支撑对应论断** | 引用一个真实但不相关的行 | 判断"支撑与否"需要模型，违背零成本目标（PLAN N2） | 中 —— 已写进两份 README |
| 5 🔴 | **扩展未加载时，模型会冒充插件的输出** | 在装有本仓库源码的目录里裸跑 `pi`（不带 `-e`），敲 `/grounded` | 无法在插件侧修复 —— pi 把无法识别的斜杠命令当普通消息发给模型，而模型读过本仓库的源码就知道该输出什么 | **高** —— 见下 |

### 关于缺陷 5

`[事实]` 2026-09-11 真实发生过：在 `<repo>` 目录裸跑 pi，敲 `/grounded`，模型先 `read` 了 `src/index.ts` 和 README，然后逐字演出了插件的输出 —— 包括 `✓ grounded on · 只读模式`、`已禁用工具: edit, write`，以及**带具体数字的** `本会话统计 — 已校验 0 · 无效 1 · 无出处 1`。

`[事实]` 会话文件里**没有任何 `custom` entry**，证明 `appendEntry` 从未被调用。全部是伪造的。

`[事实]` 它骗过了用户和我两个回合，我还据此写了一份错误的 bug 诊断。

`[判断]` **这恰好是本插件存在的理由的一次现场演示** —— 一个专门用来防止模型编造的工具，其输出本身被模型编造了。

**怎么分辨真假**：

| 信号 | 可否被模型伪造 |
|---|---|
| footer 里的 `🔍 grounded` | ❌ 伪造不了（TUI 状态栏） |
| 带背景色的提示块 | ❌ 伪造不了（模型只能输出 markdown） |
| 启动头部的 extensions 列表 | ❌ 伪造不了 |
| `notify` 的纯文本输出 | ✅ **可以伪造** |

**做任何验收之前，先确认 footer 有 `🔍 grounded`。**

---

## 5. 度量结果

原始数据在 [`NOTES.md`](NOTES.md)。这里只放结论和解读。

### 5.1 硬门槛

| | 门槛 | 实际 | 结论 |
|---|---|---|:---:|
| **T-1** | 误报率 = 0 | 117 处引用零误报；独立抽检 10/10 一致 | ✅ |
| **T-2** | 无副作用 | 全程无崩溃、无会话中断 | ✅ |
| **T-3** | `no-citation` < 30% | **0%** (0/7) | ✅ |

### 5.2 观测指标

| | A 组（n=2） | B 组（n=7） | `[判断]` 我的解读 |
|---|:---:|:---:|---|
| O-1 引用密度 | 0% | **100%** | 差异大到不需要统计检验就能看出来。但注意：A 组只有 2 题 |
| O-1 平均引用数 | 0.0 | **16.7** | 同上 |
| O-2 引用有效率 | n/a | **100%** (117/117) | 模型一个行号都没编。可能是被 prompt 第 3 条威慑住了 |
| O-3 幻觉捕获数 | — | **0** | 校验层零收获 → **K2 触发** |
| 平均工具调用 | 9.5 | **21.0** | `[判断]` 这条最有意思：模式不只让它"标注出处"，而是真的让它**读得更多**。这可能才是主要收益 |

### 5.3 样本量提醒

`[事实]` A 组 n=2，B 组 n=7。PLAN 原定各 12。

`[判断]` 这个样本量**只够支持一个结论**："开启后模型会给出处，且给的出处是真的。"任何更细的判断（比如"提升了 X 个百分点"）都不成立。

### 5.4 成本

`[事实]` 估算共约 **$6.3**，并把 Codex 订阅配额跑光。

`[判断]` 我早期用一次 trivial 调用（$0.0058）估算成本，严重低估 —— 真实的代码研究问题每题 $0.2–1.0，最贵的单题 $1.00（37 次工具调用）。**如果重来一次，我会先跑 2 题量一下真实单价再决定样本量。**

---

## 6. 代码地图

| 文件 | 行数 | 职责 | 要不要细看 |
|---|---:|---|---|
| `src/index.ts` | 268 | 注册与事件装配，**唯一有副作用的文件** | **要** —— §3 的未验证路径都在这 |
| `src/citations.ts` | 237 | 提取 / 解析 / 校验 / 判定，纯逻辑 | **要** —— 核心 |
| `src/probe.ts` | 51 | 真实文件系统访问（依赖注入的实现） | 一般 |
| `src/tools.ts` | 45 | 工具集增删运算 | 一般 |
| `src/prompt.ts` | 60 | `GROUNDED_BLOCK` 文本 | **要** —— 它决定插件好不好用 |
| `src/config.ts` | 60 | 所有常量 | 一般 |
| `test/*.test.ts` | 453 | 74 个单测 | 先做 mutation test 再决定 |

**commit 列表**

| commit | 内容 |
|---|---|
| `d32f093` | PLAN v1 |
| `4059cf8` | PLAN v2（功能简化） |
| `03e3245` | PLAN v2 五处自审修补 |
| `327d006` | VERIFY / NOTES / HANDOFF 三份文档 |
| `dbd57eb` | S0 spike 结论：改用 appendEntry |
| `e21f2ab` | **M0+M1 实现** |
| `e9096b9` | 忽略本地测试会话目录 |
| `37154b0` | README（中英）+ VERIFY 按实测修正 |

---

## 7. 后续建议

| # | 建议 | 理由 | 优先级 |
|---|---|---|:---:|
| 1 | **配额恢复后补完基线** | 现在的 n=2/n=7 撑不起结论 | 高 |
| 2 | **用一周再决定 K2** | 校验层零收获，但可能是威慑效应 | 高 |
| 3 | 把 `index.ts` 里的 `ctx: any` 换成真实类型 | 丢了类型检查 | 中 |
| 4 | 考虑给缺陷 #1 加个开关 | 通用问题被警告确实烦；可以加"只在回答里出现路径样式时才检查" | 中 |
| 5 | 发布前把 README 里的仓库 URL 占位符换掉 | `git:github.com/<you>/pi-grounded` 现在是占位 | 发布时 |

---

## 8. 如果你要接手改代码

`[判断]` 三个最容易踩的坑：

1. **不要在 `message_end` 里 `appendEntry`。** 它会落在 assistant 消息**前面**。原因和实测见 `NOTES.md` §1.3。扫描在 `message_end`，提交在 `agent_settled`。
2. **不要改成直接替换 assistant message。** 那样注释会进 LLM 上下文，每轮警告都被喂回模型。这条有对照实验，同样在 `NOTES.md` §1.3。
3. **批量脚本里调 `pi` 必须加 `< /dev/null`。** 它会读 stdin，把 `while read` 循环的剩余输入全吃掉 —— 我的第一次基线就是这么只跑了 1 题。

---

## 附：文档索引

| 文档 | 用途 | 什么时候看 |
|---|---|---|
| [`PLAN.md`](PLAN.md) | 设计决策与取舍 | 想知道"为什么这么做" |
| [`VERIFY.md`](VERIFY.md) | **验收操作手册** | **想自己确认做没做成 —— 先看这个** |
| [`NOTES.md`](NOTES.md) | 原始实测数据 | 想交叉验证我的结论 |
| `HANDOFF.md` | 本文档 | 想知道"哪里有坑" |
| [`README.md`](README.md) / [`README.zh-CN.md`](README.zh-CN.md) | 面向用户的产品文档 | 验收时不用看 |
