# pi-grounded 验收操作手册

> **这份文档的用途**：让你**不需要相信我的任何叙述**，自己动手确认插件到底做到了什么。
>
> 每条检查都是三段式：**敲什么 → 应该看到什么 → 看不到说明什么**。
> 第三段最重要 —— 只写"应该看到 X"的话，X 没出现时你分不清是坏了还是操作错了。

> ⚠️ **本文档写于实现之前**，命令按 PLAN 预期编写。每个里程碑完成后我会更新为实测命令，**所有更新处会用 `[已实测]` 标注**。没有标注的条目 = 我还没跑过，你遇到对不上属正常，直接告诉我。

---

## 0. 前置准备

### 0.1 环境要求

```bash
pi --version    # 应为 0.85.1 或更高
node --version  # 应为 v20 以上（当前 v24.19.0）
```

### 0.2 安装（开发期）

**不要用软链** —— Windows 上建符号链接需要管理员权限或开发者模式，麻烦。直接改 Pi 的用户设置：

编辑 `~/.pi/agent/settings.json`（Windows 上是 `C:\Users\<你>\.pi\agent\settings.json`），加入 `extensions` 字段：

```jsonc
{
  // ... 你原有的配置保持不动 ...
  "extensions": ["/绝对路径/pi-grounded/src/index.ts"]
}
```

> 路径用正斜杠。这样任何目录下启动 `pi` 都会加载插件，可以直接去 Pi 源码目录测试。

**临时试用**（不改设置）：
```bash
pi -e /绝对路径/pi-grounded/src/index.ts
```

### 0.3 卸载

把 `extensions` 字段删掉即可。**验收结束后如果你不打算继续用，记得删** —— 否则它会一直生效。

---

## 1. 五分钟快速验收（最重要，先做这个）

> 这一节全部是 **L1 级证据：你自己跑，不需要相信我**。

### 🔴 第 0 步：先确认扩展真的加载了

**这一步不能跳。** 2026-09-11 真实发生过：在装有本仓库源码的目录里裸跑 `pi`（忘了带 `-e`），敲 `/grounded` —— pi 把它当普通消息发给模型，模型读过本仓库源码，于是**把插件的全部输出演了出来**，包括带具体数字的统计行。骗过了两个人、两个回合。

**可靠的信号**（模型伪造不了）：

- footer 里的 `🔍 grounded`
- 启动头部 extensions 列表里的 `grounded`
- 带背景色的提示块

**不可靠的信号**：任何纯文本输出，比如 `✓ grounded on · 只读模式` —— 那是 `notify`，模型能一字不差地模仿。

**开始之前，先看 footer。** 没有 `🔍 grounded` 就说明扩展没加载，后面测什么都是假的。

### 检查 1 · 单元测试

```bash
cd /绝对路径/pi-grounded
npm install
npm test
```

- ✅ **应该看到**：全部测试通过，`citations.test.ts` 至少 20+ 个用例
- ❌ **失败说明**：核心判定逻辑有问题，后面的都不用测了，直接找我
- ⚠️ **看到 0 个测试 / 测试数很少** → 说明我偷工减料了，见 §3.1

### 检查 2 · 插件能加载

```bash
cd <任意目录>
pi
```

- ✅ **应该看到**：启动无报错，启动头部的 extensions 列表里出现 `grounded`
- ❌ **报错或列表里没有** → 路径配错，或代码有语法错误

### 检查 3 · 开关能用

在 pi 里依次敲：

```
/grounded
```

- ✅ **应该看到**：footer 出现 `🔍 grounded`，并有一条通知说明已开启及禁用了哪些工具
- ❌ **没反应** → 命令没注册成功

```
/grounded
```
（再敲一次）

- ✅ **应该看到**：footer 的 `🔍 grounded` 消失
- ❌ **还在** → 关闭逻辑有问题

```
/source
```

- ✅ **应该看到**：和 `/grounded` 行为完全一致（别名）

### 检查 4 · 只读模式真的生效

```
/grounded on
/tools
```

- ✅ **应该看到**：工具列表里 **`edit` 和 `write` 不在其中**
- ❌ **还在** → D1 只读模式没实现或失效

然后直接要求它改文件：

```
把 README.md 第一行改成 hello
```

- ✅ **应该看到**：模型说明当前处于只读模式、建议你 `/grounded off`
- ⚠️ **看到它反复重试失败的工具调用** → prompt 第 6 条没起作用，属缺陷但不致命
- ❌ **文件真的被改了** → 严重失败，只读模式形同虚设

```
/grounded off
/tools
```

- ✅ **应该看到**：`edit`、`write` 回来了

### 检查 5 · 引用校验（核心功能）· `[已实测]`

**用 `/grounded check`，完全确定性，不花一分钱，也不依赖模型配合。**

先进到一个真实的代码仓库（下面用 pi 自己的源码举例，你换成任意仓库都行，把路径换成该仓库里真实存在的文件）：

```bash
cd <你 clone 的 pi 仓库>
pi
```

```
/grounded on
/grounded check 见 packages/agent/src/types.ts:9999
```

- ✅ **应该看到**：`⚠️ 1 处引用无效`，说明是 **`文件仅 446 行，行号越界`**（数字取决于该文件当前行数）
- ❌ **提示"文件不存在"而不是"越界"** → 路径解析有问题（cwd → git 根的回退没生效）
- ❌ **没有任何输出** → 命令没注册，或者正则没匹配到

```
/grounded check 见 packages/agent/src/does-not-exist.ts:10
```

- ✅ **应该看到**：`⚠️ 1 处引用无效` → **`文件不存在`**

### 检查 6 · 不误报（硬门槛 T-1）· `[已实测]`

```
/grounded check 见 packages/agent/src/types.ts:100 和 packages/agent/src/proxy.ts:5
```

- ✅ **应该看到**：`✓ 2 处引用已校验`，并列出解析到的绝对路径
- ❌ **出现任何"无效"** → **误报，T-1 失败。这是硬门槛，一次都不能有。**

### 检查 7 · 端到端（依赖模型配合，可能需要重试）

问一个真实的代码问题：

```
Pi 的 compaction 是怎么触发的？简要说明即可。
```

- ✅ **应该看到**：模型先调用 `grep`/`read` 查源码，回答里带若干 `file:line`，**没有警告块**，footer 显示 `✓N`
- ⚠️ **出现 no-citation 警告** → 模型这轮没给出处。偶发可接受；如果每轮都这样，说明 prompt 没起作用（对应 PLAN R6 / 门槛 T-3）

> 这一条不是确定性的 —— 它依赖模型是否配合。真正的确定性验证是检查 5 和 6。

### 检查 8 · 只读模式不是摆设 · `[已实测]`

```
/grounded on
请把 README.md 的第一行改成 hello
```

- ✅ **应该看到**：模型说明处于只读模式、建议先 `/grounded off`，**且完全没有工具调用**
- ❌ **文件真被改了** → 严重失败

对照验证（证明是模式起的作用，不是模型客气）：

```
/grounded off
请把 README.md 的第一行改成 hello
```

- ✅ **应该看到**：这次它真的调用 `write` 改了文件（记得 `git checkout README.md` 还原）

### 检查 9 · 不打扰正常对话 · `[已实测]`

```
好的
```

- ✅ **应该看到**：没有任何提示块（正文短于 200 字符会被跳过）
- ❌ **出现 no-citation 警告** → 阈值逻辑有问题，插件会变成噪音源

> ⚠️ **已知行为**：开着 grounded 问一个**与代码库无关**的通用问题（比如"什么是有限状态机"），只要回答够长就会收到 `no-citation` 警告。这是 PLAN D5 的设计取舍（不做"这问题算不算代码问题"的智能判定），不是 bug。觉得烦就 `/grounded off`。

---

## 2. 分里程碑验收

> 每个里程碑完成后我会告诉你走哪一节。

### M0 · 骨架 + 只读模式

对应上面 **检查 2、3、4**，外加：

| 敲什么 | 应该看到 | 看不到说明 |
|---|---|---|
| `pi --grounded` 启动 | 启动即为开启态，footer 有 `🔍 grounded` | flag 没注册 |
| 开启状态下 `/tools` 关掉 `find`，再 `/grounded off`，`/tools` | **`find` 仍然是关闭的** | 恢复逻辑整体覆盖了用户改动（PLAN §5.2 明确要求增量恢复） |
| `/reload` | 插件重新加载，状态回到关闭，无报错、无残留 | 热重载有问题 |

### M1 · 引用校验（**主验收点**）

对应上面 **检查 1、5、6、7**，外加人工抽查：

**§2.4 独立核对**：从我的 `NOTES.md` B 组记录里**随机挑 10 处引用**，自己打开文件核对行号，与插件判定比对。

- ✅ 10 条全部一致
- ❌ 任何一条不一致 → 正则或路径解析有 bug，属 T-1 失败

### M2 · 度量与打包

| 敲什么 | 应该看到 | 看不到说明 |
|---|---|---|
| `pi install /绝对路径/pi-grounded` | 安装成功 | 包结构有问题 |
| `pi list` | 列表里有 `pi-grounded` | 同上 |
| 装完后在任意目录 `pi` → `/grounded` | 正常工作 | 打包后的入口路径不对 |
| `pi remove pi-grounded` | 干净卸载，`pi` 启动后 `/grounded` 不再存在 | 卸载残留 |

**数据核对**：打开 `NOTES.md`，确认

- [ ] 12 道基线题的**原题**都在，不是只有结论
- [ ] A 组（关闭）和 B 组（开启）的数据都有
- [ ] T-1 / T-2 / T-3 三个硬门槛的实际值已填写
- [ ] **随机抽 1 道题自己重问一遍**，结果和记录大致对得上

---

## 3. 三个"抓我"的手段

> 这几条比读任何文档都有效。

### 3.1 手工 mutation test —— 验证测试不是摆设

单测最容易造假：写一堆必过的空测试，看起来很专业。识破办法：

1. 打开 `src/citations.ts`
2. 找一个判断条件，**故意改错一行**（比如把 `line > lineCount` 改成 `line >= 0`）
3. 跑 `npm test`

- ✅ **应该看到**：**6 个测试变红**（`citations.test.ts` 4 个 + `probe.test.ts` 2 个）
- ❌ **还是全绿** → **测试是装饰品**，等于没有测试覆盖

> `[已实测 2026-09-11]` 实际结果 68 passed / 6 failed，与预期一致。

> 一分钟搞定，比读 500 行测试代码有效得多。测完记得 `git checkout src/citations.ts` 还原。

### 3.2 逐 commit 看 diff

```bash
cd /绝对路径/pi-grounded
git log --oneline
git show <commit>
```

代码量本来就小（4 个源文件），diff 是可读的。重点看：

- 有没有和 PLAN 说的不一样却没解释的改动
- `citations.ts` 里的 fs 访问是不是真的走依赖注入（否则测试没法跑）

### 3.3 交叉验证我的实测记录

`NOTES.md` 里我记的每一条都带原题。**随机抽一两题自己重新问一遍**，看结论对不对得上。

模型有随机性，答案不会字字相同 —— 你要看的是**判定结果的类别**（有没有引用、引用有没有被判无效）是否一致。

---

## 4. 验收结论记录

> 你走完之后在这里勾。有任何一条打 ❌，直接把这一节截图/复制给我。

### 硬门槛（必须全过）

- [ ] **T-1 误报率 = 0** —— 检查 6 通过，且 §2.4 抽查 10 条全对
- [ ] **T-2 无副作用** —— 全程无崩溃、无会话中断、Pi 自身功能正常
- [ ] **T-3 提示不泛滥** —— `NOTES.md` 中 B 组 `no-citation` 轮次 < 30%
- [ ] **测试有效** —— §3.1 mutation test 能让测试变红

### 功能验收

- [ ] 检查 1 单元测试全绿
- [ ] 检查 2 插件加载
- [ ] 检查 3 开关 + 别名
- [ ] 检查 4 只读模式
- [ ] 检查 5 引用校验（越界 + 文件不存在）
- [ ] 检查 6 不误报
- [ ] 检查 7 不打扰正常对话
- [ ] M0 / M1 / M2 各节表格全过

### 只有你能判断的

- [ ] **用一周之后，这插件用起来烦不烦？**

> 这条我测不了。T-1/T-3 是客观门槛，但"体验好不好"只有你说了算。
> 如果答案是"烦"，那前面全过也不算成功 —— 对应 PLAN §7 的 K2/K4。

---

## 5. 验收失败怎么办

发现问题时，把这些信息给我：

1. 哪一条检查失败（编号）
2. 你实际敲的命令
3. 实际看到的输出（原文，不要转述）
4. `pi --version` 和是否用的 `settings.json` 方式加载

> 第 3 条要原文 —— 转述会丢信息，这和插件本身要解决的问题是同一个道理。
